import { ResumeResponseDTO } from './resume-response-dto';

describe('ResumeResponseDTO', () => {
  it('should create an instance', () => {
    expect(new ResumeResponseDTO()).toBeTruthy();
  });
});
